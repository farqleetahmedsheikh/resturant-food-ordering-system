import { PlaceholderScreen } from '@/src/components/layout/PlaceholderScreen';

export default function CustomerCheckoutScreen() {
  return (
    <PlaceholderScreen
      title="Checkout"
      subtitle="Checkout will call the existing Laravel customer checkout API in the next feature phase."
      eyebrow="Customer"
    />
  );
}
