import { PlaceholderScreen } from '@/src/components/layout/PlaceholderScreen';

export default function CustomerCartScreen() {
  return (
    <PlaceholderScreen
      title="Cart"
      subtitle="Local cart state is scaffolded with AsyncStorage for non-sensitive data. Full API cart integration is ready for the next phase."
      eyebrow="Customer"
    />
  );
}
