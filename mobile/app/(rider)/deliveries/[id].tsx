import { PlaceholderScreen } from '@/src/components/layout/PlaceholderScreen';

export default function RiderDeliveryDetailScreen() {
  return (
    <PlaceholderScreen
      title="Delivery detail"
      subtitle="Status actions will call the existing Laravel rider delivery endpoint in the next phase."
      eyebrow="Rider"
    />
  );
}
