import { PlaceholderScreen } from '@/src/components/layout/PlaceholderScreen';

export default function AdminRidersScreen() {
  return (
    <PlaceholderScreen
      title="Riders"
      subtitle="Rider list and assignment controls remain in the Laravel web dashboard for now."
      eyebrow="Admin"
    />
  );
}
